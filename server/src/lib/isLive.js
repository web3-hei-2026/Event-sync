// function isLive(startTime, endTime) {
//   const now = new Date()
//   return new Date(startTime) <= now && now <= new Date(endTime)
// }

// module.exports = { isLive }
// ↑ en JS on exporte comme ça, pas avec "export default"